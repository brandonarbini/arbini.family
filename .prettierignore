# BEGIN dev-env:prettier-ignore
# pnpm owns the lockfile's formatting; reformatting it invites merge conflicts for no gain.
pnpm-lock.yaml
# END dev-env:prettier-ignore

# prisma generated client
/generated

# the Expo app formats itself from its own install
/mobile
