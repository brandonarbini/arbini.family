DATABASE_URL=postgresql://postgres:postgres@localhost:5432/arbini_family_test?connection_limit=1

# The server env schema requires the Postmark token, so the suite needs a value to parse at all.
# This is a fake: no test sends mail. Safe to commit and impossible to mask a production value
# with — only `test:env` loads this file.
POSTMARK_API_TOKEN=POSTMARK_API_TEST
